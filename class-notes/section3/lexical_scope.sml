(* 
lexical scope - functions use environment in which they're defined, not in which they're called 

function value have two parts:
- code
- environment that was current when the function was defined

these parts are called a "function closure" 
*)
