(* 

- we can use functions anywhere we can use expressions (we can pass multiple functions to a function)
- functions can be stored in data structures like tuples, records & lists
- can return functions as results of functions

"higher-order predicate" - returns true or false
*)
