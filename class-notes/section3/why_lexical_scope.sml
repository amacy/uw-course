(*
1. Function meaning does not depending on variable names
  - can change local variable names
  - can remove unused variables

2. Functions can be type-checked and reasoned about where they're called

3. (& most powerful) Closures can easily store the data they need

*)
