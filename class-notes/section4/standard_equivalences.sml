(*
1) syntactic sugar

2) consistently rename bound variables and uses

3) always be able to use a helper function without callers being able to tell

4) unnecessary function wrapping

5) if we ignore types, then ML let-bindings can be syntactic sugar for claling an anonymouse function


.... but depending on side effects of functions or environmental changes, these may not always be true


*)
