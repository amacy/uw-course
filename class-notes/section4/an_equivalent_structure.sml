(* 

A key purpose of abstraction is to allow different implementions to be equivalent

Being able to replace one module with another becomes easier when you expose less, in particular when you make types abstract

*)
